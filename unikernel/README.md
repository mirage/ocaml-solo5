This directory contains build instructions for a very simple unikernel that uses the fmt library to print "Hello World!".
The purpose of this unikernel is to test that we can link switch-installed libraries and run it.
The unikernel is built and run in CI.
